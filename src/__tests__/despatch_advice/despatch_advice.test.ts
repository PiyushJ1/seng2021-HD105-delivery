import { expect, describe, it } from "vitest";

describe("despatch advice tests", () => {
  describe("simple test", () => {
    it("should return 3", () => {
      expect(1 + 2).toBe(3);
    });
  });
});
