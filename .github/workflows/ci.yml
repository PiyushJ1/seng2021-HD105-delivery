# name: CI/CD Pipeline

# on:
#   push:
#     branches: [main]
#   pull_request:
#     branches: [main]

# jobs:
#   lint:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4
#       - name: Setup Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: 24
#           cache: "npm"
#       - name: Install dependencies
#         run: npm ci
#       - name: Run Prettier
#         run: npx prettier --check .

#   # test:
#   #   runs-on: ubuntu-latest
#   #   needs: lint
#   #   steps:
#   #     - uses: actions/checkout@v4
#   #     - name: Setup Node.js
#   #       uses: actions/setup-node@v4
#   #       with:
#   #         node-version: 24
#   #         cache: "npm"
#   #     - name: Install dependencies
#   #       run: npm ci
#   #     - name: Build
#   #       run: npm run build
#   #       env:
#   #         MONGODB_URI: ${{ secrets.MONGODB_URI }}
#   #     - name: Run tests
#   #       # add --run to ensure Vitest exits after completing tests
#   #       run: npx start-server-and-test start http://localhost:3000 'npm test -- --run'
#   #       env:
#   #         MONGODB_URI: ${{ secrets.MONGODB_URI }}
